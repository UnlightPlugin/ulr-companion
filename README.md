# ULR Companion

UNLIGHT:Revive 自訂 COST 規則與對戰追蹤的執行端。

依《ULR Cost Rules Integration Plan Spec v0.3》建置。三方分工：

| 元件              | 負責                                           | 在哪                  |
| ----------------- | ---------------------------------------------- | --------------------- |
| **ULGG**          | 公開規則登錄、版本治理、Room 驗證、戰果統計    | <https://ulgg.online> |
| **GitHub**        | 原始碼、Schema、版本契約、CI、Release          | 這個 repo             |
| **ULR Companion** | 規則計算、CDP 觀測、遊戲內顯示、回報與自動更新 | 這個 repo             |

---

## 現在的狀態

**v1.0.0 已發布。** 插件端要做的工作包都可用了；還標 🔲 的是 ULGG 端的東西，
或是這邊只有型別、等對面實作的契約。

| 工作包 | 內容                                            | 狀態                        |
| ------ | ----------------------------------------------- | --------------------------- |
| WP-01  | Schema／Canonical JSON／SHA-256／跨語言測試向量 | ✅ 可用                     |
| WP-02  | Cost Engine                                     | ✅ 可用                     |
| WP-03  | ULGG 規則資料表與 GET API                       | 🔲 ULGG 端                  |
| WP-04  | Room Report API                                 | 🔲 只有型別                 |
| WP-05  | Match Import 擴充                               | 🔲 只有型別                 |
| WP-06  | Character Analysis 版本篩選                     | 🔲 ULGG 端                  |
| WP-07  | CDP UI                                          | ✅ 可用                     |
| WP-08  | Release／自動更新                               | ✅ 可用（發布來源已上線）   |
| WP-09  | WS 事件層                                       | ✅ 可用                     |
| WP-12  | 移動階段仲裁（單邊）                            | ✅ 可用                     |
| WP-15  | 中間人握手＋約定秒數＋托盤                      | ✅ 可用（雲端中間人已上線） |
| WP-16  | 自動配對＋跨版本 COST 相容                      | ✅ 可用                     |

先做 WP-01 是因為它是附錄 B 裡**唯一沒有依賴**的工作包，其他全部卡在它後面。
契約定下來，插件端與 ULGG 端才能平行開發而不反覆重寫（規格書 §15）。

---

## 開始

需要 Node.js 20.11 以上。

```bash
git clone https://github.com/UnlightPlugin/ulr-companion.git
cd ulr-companion
npm install
npm run verify      # format + lint + typecheck + test
```

`npm run verify` 全綠就代表環境沒問題，可以開始接工作包了。

試跑一份規則：

```bash
npx tsx apps/companion/src/index.ts packages/rule-schema/test-vectors/rules/arcadia-balance-1.2.0.json
```

### 移動階段仲裁（WP-15）

托盤程式，一份管一個遊戲客戶端。

```powershell
npm run tray                      # 上次用的那份配置
npm run tray -- --profile <id>    # 指定配置
npm run tray -- --port 59222       # 相容用法：對得上就用那份，對不上開臨時的
```

⚠ **中間人預設是雲端的那一台**（見下面「這個插件會連到哪裡」）。要在同一台
電腦上讓兩份插件互相握手（雙開測試），把配置裡的「中間人」填 `local` ——
那時先開的那份會自動當 broker，不必再多開一個視窗。

**打包成安裝檔：**

```powershell
npm run dist        # → out/release/ULR Companion Setup 0.1.0.exe
```

NSIS、`oneClick`、`perMachine: false`（裝到 `%LOCALAPPDATA%`，更新不需要 UAC ——
裝到 `Program Files` 的話**每次更新都會跳 UAC**，玩家會以為中毒）。
圖示是程式現畫的漩渦，repo 裡沒有 `.ico`。

安裝檔 **82 MB**，解壓後 284 MB。這個大小幾乎全部是 Electron 自己：

|                            | 大小       |
| -------------------------- | ---------- |
| 我們的程式碼（`app.asar`） | **0.4 MB** |
| Chromium 主程式            | 200 MB     |
| GPU／著色器 DLL            | 42 MB      |
| ICU、授權條款、資源 pak    | 38 MB      |
| locale pak（5 種語言）     | 2.9 MB     |

已經做掉的：**locale 從 55 種砍到 5 種**（44 MB → 2.9 MB）、sourcemap 不進包、
`compression: maximum`。再往下就只剩 GPU 相關的 DLL，而砍掉它們的風險是
「某些機器上畫面靜靜地不出來」—— 對一個要在對戰中一直開著的工具不值得。

⚠ **Electron 的下限大約就是 70 MB**。真要做到 5–10 MB 得換成 Tauri／WebView2，
而那表示主程序要用 Rust 重寫 —— CDP adapter、仲裁引擎、中間人全部是 TypeScript，
不成比例。

⚠ **還沒有程式碼簽章**，首次安裝會跳 Windows SmartScreen「不明的發行者」。
後續更新不會再跳（安裝器由已安裝的程式啟動，不帶 Mark-of-the-Web）。
下載頁要老實揭露這件事並附安裝檔的 SHA-256，**不要教玩家關掉系統防護**（§10.3）。

⚠ **從 VS Code 的終端機啟動打包版會失敗**，錯訊是 `bad option: --port`。
那是 `ELECTRON_RUN_AS_NODE` —— VS Code 的 extension host 會設它，Electron 的 exe
於是被當成純 Node 跑。開發時用 `npm run tray`（`scripts/run-tray.mjs` 會清掉它），
或從捷徑／檔案總管開。一般玩家不會遇到。

**多開是進階功能，預設關閉。** 一般玩家裝完就是一個視窗管一個遊戲客戶端，
不需要知道「配置」這回事 —— 要打開它得自己去 設置 › 關於 最底下勾「進階」，
「配置」才會出現在設置的**最後一格**（優先級最低）。

打開之後，設定 › 配置維護一張清單，按「開新實例」就再開一個視窗綁另一份 ——
埠寫在命令列的話，一般玩家永遠開不了第二個。新增時會自動避開已被佔用的埠：

| 名稱   | 客戶端 | 遊戲埠 | 狀態     | 中間人   |
| ------ | ------ | ------ | -------- | -------- |
| 主帳號 | 桌面版 | 59222  | 對戰中   | （雲端） |
| 小號   | 網頁版 | 59223  | 遊戲開著 | （雲端） |

⚠ **兩份配置不能共用同一個遊戲埠**（Windows 的目錄鎖會擋掉第二份，症狀是
「按了開新實例但什麼都沒發生」）。

「狀態」欄只說**那個埠上有沒有遊戲在回話**（主程序每四秒敲一次
`/json/version`）。**它回答不了「另一份有沒有插件在管它」** —— 那是另一個
process，這個視窗問不到。自己那一列才是引擎的真實狀態。

畫面照里程碑分成六個大分頁，各自有側邊子分頁：

| 分頁 | 子分頁                                  | 狀態                                  |
| ---- | --------------------------------------- | ------------------------------------- |
| 戰鬥 | 準備 · 秒數上限                         | ✅ 可用                               |
| 戰鬥 | 加速                                    | 🟡 做好了，**刻意先不開放**           |
| 戰鬥 | 記牌 · 牌譜                             | 🔲 前瞻預告                           |
| 牌組 | Cost 表                                 | ✅ 可用（角色·怪物·裝備·事件卡）      |
| 牌組 | 編輯 COST                               | ✅ 可用（四個分頁，顯示卡名不是編號） |
| 牌組 | 配對                                    | ✅ 可用（手動開房＋自動配對）         |
| 牌組 | 隱藏地圖                                | ✅ 可用（010~013 放回開房選單）       |
| 渦   | 速度加倍 · 推薦隊伍 · 排名與獎勵        | 🔲 前瞻預告                           |
| 獎勵 | 未來視                                  | 🔲 前瞻預告                           |
| 任務 | 寶箱標註                                | 🔲 前瞻預告                           |
| 設置 | 配置 · 連線 · 啟動 · 更新 · 記錄 · 關於 | ✅ 可用                               |

未實作的頁不是「敬請期待」—— 每一頁都寫清楚**卡在哪**。

外觀是**純 CSS 重現**的 Unlight 風格，色票量自跑著的客戶端
（分頁 `#622b2f`／hover `#873136`、底 `#1e2336`）。系統匣圖示是程式現畫的菱形
（深紅描邊＋狀態色）。repo 裡**沒有任何遊戲圖檔**，也沒有官方美術 ——
這既是設定視窗能維持 `default-src 'none'` 的原因，也避免第三方插件看起來像官方出品。

遊戲裡的 OK 鈕預設**維持官方原本的樣子**（不染色）；要視覺提示的話，
戰鬥 › 準備 可以改成琥珀色或自訂顏色。

同一套東西也有命令列版本，適合看細節：

```powershell
npx tsx apps/companion/src/index.ts arbiter --port 59222 --phase-seconds 15
```

⚠ **這會改變遊戲行為**，而且有兩條路會送出 `I_am_ok`（準備放行、約定秒數）。
動手前先讀 [docs/arbiter-link.md](docs/arbiter-link.md) —— 裡面每一條「⚠」都是
實測踩過的失效模式。

#### 會改變勝負的功能都是「雙方同意才生效」

| 功能     | 協商 | 沒配到對手時         |
| -------- | ---- | -------------------- |
| 準備     | and  | 退回「誤按反悔」窗口 |
| 秒數上限 | max  | 完全不縮短           |
| 加速     | min  | 原速                 |

⚠ **加速引擎做好了，但 UI 刻意不開放。** 它改的是對戰的節奏，而節奏直接關係到
誰有多少時間思考；實測收益只有約 **1 分鐘／場**（伺服器排程那段動不了），
判斷錯的代價卻是公平性 —— 這個比例不值得先開放再修。設定頁那一格是前瞻預告。

#### 插件當掉時，遊戲會回到原本的樣子

| 注入的東西    | 插件當掉                     | 靠什麼               |
| ------------- | ---------------------------- | -------------------- |
| 準備（攔 OK） | **這個階段撐完，下一回取消** | 心跳 3 秒 → 降級模式 |
| 加速          | 10 秒內自己還原成原速        | 租約                 |

⚠ 準備那條刻意**不當場取消**。舊行為是心跳一過期就把壓著的 `I_am_ok` 送出去 ——
玩家剛按下 OK、以為「再按一次就取消」，那次按壓卻在他不知情時定案了。現在改成
頁面自己接手單邊的部分（再按一次仍可取消、剩 3 秒自己送出），到**下一個階段**
才真的關掉。開關只在階段邊界改變，永遠不會在玩家操作到一半時變。

不對稱是刻意的：**開啟隨時生效，關閉只在階段邊界** —— 關閉有傷害，開啟沒有。

### 自動配對（WP-16）

牌組 › 配對 那一頁，按「開始自動配對」就會去中間人那邊排隊，配到條件一樣的人
就**自動開房／進房**。⚠ 配到之後會直接消耗 AP 並開打，所以那顆按鈕只能是玩家
親手按的，插件不會自己開始。

排在同一條隊伍的條件有四個：**規則族、頻道、3vs3、約定 COST 上限**。填得不一樣
的人算出不同的配對鍵，落在不同的 Durable Object，物理上就配不到對方。

⚠ **「規則族」不是「規則的核對碼」。** 同一套規則的不同版本會排在同一條隊伍裡：

> 兩份規則只差在最新角色的定價，而雙方都沒帶那隻上場 —— 那個差異對這一場沒有
> 任何可觀測的影響，卻會讓兩人永遠配不到對方。規則作者每發一次版就把社群切成
> 兩半，而切開的理由是一個沒有人帶上場的角色。

所以配到人之後，兩邊各自用**自己那份規則**把**兩副牌**都算一次，交換計算結果的
指紋；四個指紋兩兩相等才開打。整套推導、協定與它的極限在
[docs/match-making.md](docs/match-making.md)。

| 配對條件                  | 誰判                       | 用哪個 COST    |
| ------------------------- | -------------------------- | -------------- |
| 遊戲的「牌組Cost限制 ±N」 | 伺服器                     | **原版**       |
| 約定 COST 上限            | 插件，**各自檢查自己那副** | **你選的規則** |

兩個是不同的數字，配對頁上並排顯示 —— 混在一起看會做出完全錯誤的判斷。

**約定上限想填幾就填幾**，但配對頁也把該頻道的官方 COST 檔位列成快速鍵
（平常對戰最常約的就那幾檔）。⚠ 迪特赫姆（2）與布萊德克洛伊茲（4）**自己沒有
檔位**，借的是同一組排名頻道的 —— 而兩組的數字不一樣（實測 1 是 57/66/78、
3 是 56/69/71）。這些值**每週二遊戲更新會變**，所以插件每次都現讀，不快取。

頻道本身也進配對條件，而且會檢查兩次：按下按鈕時（沒進頻道就擋），以及**每次
配到人時**（排隊途中換頻道的話會停下來告訴你）—— 房間清單是分頻道推播的，
不在同一個頻道就根本看不到對方的房。

---

## 專案結構

```
packages/
  rule-schema/     WP-01  規則格式、正規化、內容雜湊 ← 所有東西的依賴
  api-contract/           ULGG API 的型別契約（純型別，無實作）
  cost-engine/     WP-02  COST 計算            ← 壓 C 已對齊原版
                   WP-16  這一場的語義指紋     ← 跨版本相容判在這裡
  cdp-adapter/     WP-07  連遊戲、解析事件、注入畫面
  arbiter-link/    WP-15  中間人協定、broker、客戶端（協定是純函式）
                   WP-16  配對佇列的協定與客戶端
  arbiter-engine/  WP-15  四個零件的生命週期，CLI 與托盤共用同一份
                   WP-16  配對的狀態機（排隊 → 驗算 → 開房／進房）
apps/
  companion/              命令列子指令（probe / cost / official-rule / pack / unpack / watch / arbiter / speed / web）
  tray/            WP-15  托盤程式，一份管一個客戶端（具名配置 = 多開）
rules/
  unlight-official-*.ulrcost.json   從客戶端匯出的原版 COST 表，四張表全帶
                                    （角色·怪物·裝備·事件卡；fork 它來做自訂規則）
  tomorin-squeeze-band-*.ulrcost.json 夾擠式罰C：逐點遞增的壓 C 曲線，最小單位 0.5C
                                    ⚠ 一份規則只留一個檔（.rule.json 是 unpack
                                    的產物，不進版控 —— 兩個檔會被誤認成兩份規則）
docs/
  open-questions.md       開工前要先定案的事
  official-cost-rule.md   原版怎麼算 COST —— 出處是客戶端本體，不是推測
  cost-rule-v2-dsl.md     schemaVersion 2 草案：主 C／輔助、協同、容許曲線
  canonical-json.md       給 ULGG（PHP）實作同一套正規化用
  release.md              WP-08：打包、自動更新、系統匣設計
  launching.md            怎麼把遊戲開起來、雙開要注意什麼
  battle-events.md        WS 事件目錄（雙邊實測，A/B 是座位不是敵我）
  battle-features.md      戰鬥四項功能的規劃與相依順序
  battle-timing.md        對戰時間花在哪 —— 「能不能加速」的答案
  battle-preplay.md       預先出牌：攻防是循序的，中間人不需要
  handoff-wp12.md         WP-12 交接
  arbiter-link.md         WP-15：中間人握手、約定秒數、托盤、自動更新
  match-making.md         WP-16：自動配對、跨版本相容、中間人看得到什麼
```

跨 package 直接用 `@ulr/xxx` import，指向原始碼，clone 下來不用先 build。

---

## 核心概念：規則的身分是它的 Hash

一份規則的識別不是名字也不是版本號，是**內容的 SHA-256**。

```
規則 JSON → Schema 驗證 → Canonical JSON → SHA-256 → contentHash
```

因為「同樣叫 1.2.0 但內容被人改過」的兩份規則必須被判定為不同，否則
Room 一致性檢查就是假的。正規化採 **RFC 8785 (JCS)**，規格書 §5.2 要求的
四件事（鍵值排序、UTF-8、消除非語義空白、統一數值表示）正是它的定義。

```ts
import { contentHash, validateCostRule } from "@ulr/rule-schema";

const result = validateCostRule(json);
if (result.valid) {
  console.log(contentHash(result.rule)); // sha256:bebd49f8…
}
```

⚠ **ULGG 端（PHP）要實作同一套正規化，先讀 [docs/canonical-json.md](docs/canonical-json.md)。**
`json_encode(21.0)` 在 PHP 給 `21.0`、JCS 要求 `21`，這一個差異就會讓兩邊
所有 Hash 全部對不起來。`packages/rule-schema/test-vectors/canonical.json`
是語言無關的驗收向量，照著跑就對得上。

⚠⚠ **但 contentHash 不是配對的閘門。** 它回答的是「兩份規則是不是同一份」，
而配對要問的是「**這一場**算出來的東西一不一樣」—— 兩份規則只差在雙方都沒帶
上場的角色時，答案是「一樣」。所以同一套正規化被用了兩次：一次蓋整份規則
（版本識別），一次蓋一場對局的計算結果（配對閘門）。第二個叫
`evaluationHash`，在 `@ulr/cost-engine`，推導見
[docs/match-making.md](docs/match-making.md)。

---

## 這個插件會連到哪裡、送什麼

**只有兩個對外連線**，兩個都到同一台伺服器（Cloudflare Workers，原始碼在
[`apps/link-worker`](apps/link-worker)）：

| 連線         | 何時                       | 送出去的東西                        |
| ------------ | -------------------------- | ----------------------------------- |
| 中間人       | **對戰中才連**             | 雜湊過的場次編號 + 你的四個偏好設定 |
| **配對佇列** | **按下「開始自動配對」後** | 雜湊過的配對條件；⚠ 見下面那段      |
| 檢查更新     | 每小時一次                 | 什麼都不送，只問「最新版是哪一版」  |

**不會送出去的東西**：你的名字、Steam 帳號或 token、對手是誰、任何一張手牌、
對戰結果。

場次編號是遊戲那個 32 字元 room id 的 **SHA-256 前 64 bit**（`roomKey()`）。
伺服器要回答的問題只有一個 ——「**這兩條連線在不在同一場對戰**」—— 而雜湊
足以回答它。所以「伺服器看得到什麼」是**數學問題，不是信任問題**：它認不出
那是哪一場，也認不出你是誰。

⚠ 沒進對戰時側通道**完全不連線**。中間人連不上時自動退回單邊模式 —— 功能會少
（秒數不縮短、準備功能退回「誤按反悔」窗口），但不會壞。

#### ⚠ 自動配對是唯一會送出牌組內容的地方，而且只在一種情況下

配對佇列平常送的都是雜湊：配對鍵（規則族＋頻道＋3vs3＋上限）、規則標籤。
但配到的對手**用另一個版本**的規則時，兩邊要交換一份**牌組描述子**才驗算得了
「這一場算出來的東西一不一樣」：

```jsonc
{ "v": 1, "characters": ["cc001_r03", "cc078_04", "cc205_02"] }
```

就是三個角色代號，沒有等級以外的東西、沒有身分、沒有手牌。它經過中間人轉發
（中間人不解讀，但**它經手**）。對手的插件收到之後只拿去算指紋，**不顯示、
不記錄、沒有任何介面問得到它**。

規則版本相同時（絕大多數的配對）**完全不會走到這一步**。

⚠ 這條的保證強度跟房號那條**不一樣**，不要混為一談：房號是雜湊的，
「中間人認不出那是哪一場」是數學；描述子是明文轉發的，「中間人不看」是行為。
另外這些角色在幾秒後就會由遊戲自己廣播給整個大廳（房間清單本來就帶雙方牌組
縮圖）。完整的取捨寫在 [docs/match-making.md](docs/match-making.md) 第 7 節。

自己架一台也可以：`apps/link-worker` 是完整的，把配置裡的「中間人」指過去就好。

---

## 給玩家

**v1.0.0 是第一個正式版**（在此之前的 0.x 全是內測）。發布來源已經上線，插件會
自己更新（每小時檢查一次，**下載完不會在對戰中套用**）。

裝之前先知道三件事：

1. **Windows 會跳「不明的發行者」。** 安裝檔還沒有程式碼簽章（憑證要錢，
   目前還沒買）。點「其他資訊 → 仍要執行」。不放心的話原始碼全部在這裡，
   自己 `npm run dist` 打包出來的是同一份東西，Release 頁也附了 SHA-256
   可以自行核對。
2. **要先在 Steam 設一次 debug port**，否則插件永遠接不上（畫面只會寫
   「等遊戲…」）。步驟寫在插件的 **設置 › 連線** 那一頁，照著做一次就好。
3. **它會改變勝負。** 準備功能與秒數上限都是**雙方都裝了才生效**的東西，
   而且協商永遠取「對雙方都不更嚴格」的那一邊 —— 沒有任何設定能單方面
   縮短對手的思考時間。詳見下面的公平性立場。
4. **自動配對會替你開房與進房**，也就是**消耗 AP 並直接開打**。它只會在你
   親手按下「開始自動配對」之後才動，而且「停止」隨時可以按。

回報問題時請附上**版本號**（視窗左上角那個 `v1.0.0`）與 **設置 › 記錄**
那一頁的內容 —— 那裡面不含任何遊戲內容，可以直接貼。

---

## 公平性立場

這個插件只改**自己客戶端的顯示**，伺服器收到的東西一個字都沒變。

- 不修改伺服器端技能判定，不繞過官方戰鬥規則
- 自訂 COST 是**自我約束**：雙方同意才有意義，亞城／官方環境完全不受影響
- 不顯示也不上傳對手未揭露的手牌、牌組或任何隱藏資訊
- 對局限制條款（`restrictions`）只顯示給人看，引擎不強制、也不解析條件字串

⚠ 第三條在**跨版本自動配對**時有一個要講清楚的例外：驗算「這一場算出來的東西
一不一樣」需要兩邊的出戰角色，所以插件會交換一份三個角色代號的描述子。它**不
顯示給玩家看**（畫面與記錄裡都沒有，也沒有介面問得到），純粹拿去算指紋 ——
因為看得到對手帶什麼就能挑對手，而那會讓整個約戰功能失去意義。規則版本相同時
根本不會交換。牌組本來也不是隱藏資訊（遊戲的房間清單就把雙方牌組廣播給大廳
每一個人），但「不是隱藏資訊」不等於「可以拿來挑對手」。

詳見規格書 §12。任何 PR 若違反上述任一條，不會被合併。

---

## 授權

MIT。見 [LICENSE](LICENSE)。
