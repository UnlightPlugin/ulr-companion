/**
 * 發布簽章的公鑰（烤進客戶端）
 * ==============================
 * 對應的**私鑰不在這個 repo 裡，也不該在任何地方的 CI 或伺服器上**。
 * 它離線保管，只在你本機發版時用一次（`scripts/sign-release.mjs`）。
 *
 * ⚠ **換這把金鑰要非常小心。** 已經裝在玩家電腦上的舊版帶的是舊公鑰 ——
 * 你一換，他們就再也驗不過任何一份新清單，等於**所有舊版永遠收不到更新**，
 * 而且畫面上什麼都不會說（驗不過就是安靜地當作「這次沒更新」）。
 * 唯一的救法是叫每個人手動重新下載。
 *
 * 所以：**私鑰遺失 = 所有既有安裝失去更新能力。** 備份它，離線備份。
 *
 * 產生方式（只做一次）：
 *
 * ```
 * node scripts/keygen-release.mjs
 * ```
 *
 * 它會把私鑰寫到你指定的路徑（預設 `%USERPROFILE%\.ulr-release-key\`），
 * 並把公鑰印出來讓你貼到這裡。`.gitignore` 已經擋掉 `*.pem`，
 * 但**不要把私鑰放進專案資料夾**，那只是多一層不必要的機會。
 */

/**
 * SPKI PEM 格式的 Ed25519 公鑰。
 *
 * ⚠ **空字串 = 還沒產生金鑰**，這時 `updater.ts` 會**拒絕所有更新**並在記錄裡
 * 留一行。那是刻意的預設：沒有信任根的時候，正確的行為是不更新，
 * 不是「先相信再說」。
 */
export const UPDATE_PUBLIC_KEY =
  "-----BEGIN PUBLIC KEY-----\n" +
  "MCowBQYDK2VwAyEAmBKA2Mspb4ydnEw/NmMKdCosEw3aAgaK7lqUQ4CgUJM=\n" +
  "-----END PUBLIC KEY-----\n";
